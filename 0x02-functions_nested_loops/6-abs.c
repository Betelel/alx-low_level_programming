#include "main.h"

/**
 * _abs - pirntsints theabsolute functio of the numebr
 * @n: the number to be evvaluated
 * Return: the abslolute value of the number
 */
int _abs(int)
{
	int n;

	if (n > 0)
		return (n);
	else
		return (-n);
}
